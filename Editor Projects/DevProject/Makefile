# Alternative GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  DevProject_config = debug

else ifeq ($(config),release)
  DevProject_config = release

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := DevProject

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

DevProject:
ifneq (,$(DevProject_config))
	@echo "==== Building DevProject ($(DevProject_config)) ===="
	@${MAKE} --no-print-directory -C . -f DevProject.make config=$(DevProject_config)
endif

clean:
	@${MAKE} --no-print-directory -C . -f DevProject.make clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   DevProject"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"