10/01/2023 (my birthday yay)

-The Logger now has 2 sinks, the default stdou_color sink and the ringbuffer sink for getting
 the last messages
-Added GetLastLogMessages function to Logger.h, returns a vector of the last messages Logged
 (includes C# Scripts calling Log functions)
-The Format of the LastLogMessages is "level:message"

-Added the Console Window, Displays the Log messages and also colors them accordingly
 based on their level